import styled from 'styled-components';

const SearchForm = styled.form`
    width: 100%;
    display: flex;
    flex-direction: row;
    padding: 10px 10px;
    background-color: white;
`;


export default {
    SearchForm
}